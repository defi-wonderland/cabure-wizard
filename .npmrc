save-exact=true
# cli depends on cabure-crypto by plain version (npm publish ships it verbatim;
# workspace: would break downstream installs). Link it locally by version match.
link-workspace-packages=true
